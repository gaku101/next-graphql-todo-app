scalar AWSTimestamp

type Todo {
  id: ID!
  title: String!
  completed: Boolean!
  createdAt: AWSTimestamp!
}

type Query {
  getTodos: [Todo!]!
}

input AddTodoInput {
  title: String!
}

type Mutation {
  addTodo(addTodoInput: AddTodoInput!): Todo
}
